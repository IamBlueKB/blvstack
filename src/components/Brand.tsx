// BLVSTACK wordmark — React version. Uses Λ (Greek capital Lambda) as the stylized 'A'.

export default function Brand() {
  return (
    <span aria-label="BLVSTACK" style={{ whiteSpace: 'nowrap' }}>
      <span aria-hidden="true">BLVSTΛCK</span>
    </span>
  );
}
